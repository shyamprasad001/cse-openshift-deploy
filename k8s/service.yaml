apiVersion: v1
kind: Service
metadata:
  name: springboot-openshift-service
spec:
  selector:
    app: springboot-openshift
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP